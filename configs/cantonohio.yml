root_urls:
  - https://cantonohio.gov/
max_depth: 4
refresh_days: 7
robots: true
# Fetch mode (optional):
# - http: direct HTTP GET (default)
# - headless_chromium: render HTML via headless Chromium (Playwright)
fetch_mode: http
# Optional schedule (example: weekly at 02:00 UTC)
# schedule: "0 2 * * 0"
